{% if dnsserver.ip != ansible_default_ipv4.address %} port=0 {% endif %}
dhcp-range={{internal_subnet}},proxy 
dhcp-boot=pxelinux.0,{{ansible_default_ipv4.address}},{{internal_subnet}}
pxe-service=x86PC,"netboot",pxelinux
enable-tftp
tftp-root={{pxe_tftpdir}} 
